services:
  ephemeral-bot:
    build: .
    container_name: ephemeral-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      # Persist per-guild settings (announce/song channels, ping role, voice
      # channel) across container rebuilds and recreations.
      - ./data:/app/data
